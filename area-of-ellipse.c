#include <stdio.h>
int main(void){
    float a,b;
    float pi = 3.1415;
    printf("input a and b sep by , ");
    scanf("%f, %f",&a,&b);
    printf("Area of ellipse is %f",(pi*a*b));
    return 0;

}